<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#A30202"/> <!-- Background color -->

    <!-- Specify the corner radius for all corners -->
    <corners android:radius="20dp"/>

    <!-- Optional: Add padding inside the shape -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>

    <!-- Optional: Add border -->
    <!--    <stroke
            android:width="2dp"
            android:color="#000000"/> &lt;!&ndash; Border color &ndash;&gt;-->
</shape>